<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- The subtitle line is drawn over the media player. No network permission: nothing leaves the device. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- The apps that can take a word: dictionaries, translators, flash card apps. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT" />
            <data android:mimeType="text/plain" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Android gives the play position of other apps only to a notification listener.
            This service reads no notification. The system keeps it running, so the overlay
            needs no foreground service and no notification of its own.
        -->
        <!--
            Gives the position of the player to a reader app, and takes play, pause and seek from
            it. KOReader with the SubRead plugin uses this. See PlayerProvider.
        -->
        <provider
            android:name=".PlayerProvider"
            android:authorities="space.subread.overlay.player"
            android:exported="true" />

        <service
            android:name=".MediaListener"
            android:exported="true"
            android:label="@string/listener_label"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
    </application>
</manifest>
